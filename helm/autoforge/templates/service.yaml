apiVersion: v1
kind: Service
metadata:
  name: autoforge-app-svc
  namespace: {{ .Values.namespace }}
spec:
  type: NodePort
  selector:
    app: autoforge-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
